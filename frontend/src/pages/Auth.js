import { useHistory } from "react-router-dom";
import firebase from "firebase";

function Auth() {
  const history = useHistory();

  return (
    <div>
      <h1>Sign In</h1>
    </div>
  );
}

export default Auth;
